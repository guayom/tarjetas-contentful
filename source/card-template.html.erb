---
page_classes: main
---
<%= partial "partials/card/hero", :locals => { :titulo => titulo, :producto => producto} %>

<section class="feature">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-6 text">
        <i class="icon-circle icon-circle-user"></i>
        <h2 class="title-secondary">dirigida a los que quieren ahorrar</h2>
        <p>Las tarjetas Cashback se dirigen a personas a las que les gusta comprar en forma segura y práctica,  y acumular dinero por las compras realizadas.</p>
      </div>
    </div>
  </div>
</section>

<section>
  <div class="nav-holder">
    <div class="nav-wrap">
      <div class="row" id="section-tarjetas">
        <hr />
        <p class="descrip">Elegí la tarjeta que más te conviene</p>
      </div>
      <div class="visible-xs">
        <div class="container">
          <div class="tab-active-mobile pull-left">
            <img class="hidden-sm" src="/images/tarjeta-icon-clasica.png" alt="" />
            <span>Cashback clásica</span>
          </div>
          <button type="button" class="nav-wrap-btn">
            <span class="line"></span>
          </button>
        </div>
      </div>
      <nav class="nav nav-cards" >
        <div class="container">
          <% get_variations_by_color(producto).each_with_index do |variacion, index| %>
            <a role="presentation" href="#tab-<%= index + 1 %>" aria-controls="tab-<%= index + 1 %>" role="tab" data-toggle="tab" class="<%= "active" if index == 0 %>">
              <img class="hidden-sm" src="<%= variacion.arte ? "#{variacion.arte.url}?w=55" : "https://placehold.it/55x34" %>" alt="" />
              <%= "#{producto.titulo} #{variacion.color.titulo}" %>
            </a>
          <% end %>
          <div class="button hidden-xs">
            <a href="<%= producto.form %>" class="btn btn--green"><%= data.links.cta %></a>
          </div>
        </div>
      </nav>
    </div>
  </div>
  <div class="tabs">
    <% get_variations_by_color(producto).each_with_index do |variacion, index| %>
      <div role="tabpanel" class="tab-pane<%= " active" if index == 0 %>" id="tab-<%= index + 1 %>">
        <div class="tabs-heading">
          <p class="text-center">Para obtener esta tarjeta debes contar con un Ingreso mínimo de <%= get_salario_minimo(producto, variacion) %> netos</p>
        </div>
        <div class="tabs-content">
          <div >
            <div class="container">
              <h2 class="title-secondary"><i class="icon-circle icon-circle-graph"></i> Beneficios</h2>
              <%= partial 'partials/card/beneficios', :locals => { :producto => producto } unless producto.beneficiosGenerales.nil? %>
              <div class="row">
                <div class="col-xs-12 col-sm-4">
                  <img class="feature-icon" src="/images/icon-cards.png" alt="" />
                  <p class="feature-text">
                    <% if get_product_brands(producto).count > 1 %>
                      <%= "Este programa se encuentra disponible en las marcas #{get_product_brands(producto).join(', ')}".gsub(/,(?=[^,]+$)/, " y ") %>
                    <% else %>
                      Este programa se encuentra disponible únicamente en la marca <%= get_product_brands(producto)[0] %>
                    <% end %>
                  </p>
                </div>
                <div class="col-xs-12 col-sm-8 text-center">
                  <div class="card-image">
                    <img src="<%= variacion.arte.url %>?w=300" alt="<%= variacion.titulo %>" />
                  </div>
                </div>
              </div>
            </div>
          </div>
          <% ###########################################Agregar aquí beneficios exclusivos %>
        </div>

        <%= partial "partials/card/condiciones", :locals => {:producto => producto} %>

        <section class="tasa-intereses">
          <div class="container">
            <div class="row">
              <div class="col-xs-12 col-sm-5">
                <h2 class="title-secondary"><i class="icon-circle icon-circle-graph"></i> Tasa de intereses</h2>
                <p>Nullam id dolor id nibh ultricies vehicula ut id elit. Duis mollis, est non commodo luctus, nisi erat porttitor ligula, eget lacinia odio sem nec elit.</p>
              </div>
              <div class="col-xs-12 col-sm-7">
                <div class="cards">
                  <div class="card blue">
                    <h3 class="blue">Tasa Preferencial mensual</h3>
                    <div class="card-content">
                      <div class="card-left">
                        <h4 class="blue">Colones</h4>
                        <strong><%= variacion.color.tasaPreferencialColones %>%</strong>
                      </div>
                      <div class="card-right">
                        <h4 class="blue">Dólares</h4>
                        <strong><%= variacion.color.tasaPreferencialDolares %>%</strong>
                      </div>
                    </div>
                  </div>
                  <div class="card purple">
                    <h3 class="purple">Tasa Normal mensual</h3>
                    <div class="card-content">
                      <div class="card-left">
                        <h4 class="purple">Colones</h4>
                        <strong><%= variacion.color.tasaNormalColones %>%</strong>
                      </div>
                      <div class="card-right">
                        <h4 class="purple">Dólares</h4>
                        <strong><%= variacion.color.tasaNormalDolares %>%</strong>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div class="disclaimer">
              <h2>Donde no Acumula</h2>
              <p><%= data.tarjetas.generalidades.Y230IMEza66248CIewMmu.dondeNoAcumula %></p>
            </div>
          </div>
        </section>
      </div>
    <% end %>
  </div>
</section>


<section class="banner">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-9 col-md-8">
        <h2 class="title">¿Estás listo para acumular dinero con tus compras?</h2>
        <p class="description">Solicitá hoy mismo tu tarjeta <%= producto.titulo %> y empezá a ahorar.</p>
      </div>
      <div class="col-xs-12 col-sm-3 col-md-3 col-md-push-1">
        <a href="#" class="btn btn--green btn--big"><%= data.links.cta %></a>
      </div>
    </div>
  </div>
</section>
